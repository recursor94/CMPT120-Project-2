function officePuzzleTest() {
    if(hasItem("mysterious blueprints")) {
        updateText("There is a decoder here--the serial number on the label"
                   + "matches the serial number on your blueprints");
        }
        

}